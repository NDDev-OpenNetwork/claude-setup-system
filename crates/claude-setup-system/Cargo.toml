[package]
name = "claude-setup-system"
version = "0.1.0"
description = "Installs, reselects, restores and removes a complete Claude Code harness configuration in a caller-named target directory."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[[bin]]
name = "claude-setup-system"
path = "src/main.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
setup-core = { workspace = true }
provider-v3 = { workspace = true }

[lints]
workspace = true
